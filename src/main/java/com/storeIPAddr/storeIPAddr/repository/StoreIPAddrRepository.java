package com.storeIPAddr.storeIPAddr.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.storeIPAddr.storeIPAddr.Entity.StoreIPAddr;

public interface StoreIPAddrRepository extends JpaRepository<StoreIPAddr, Long> {

}
